# Segurança
A senha nunca é solicitada no terminal. Perfil autenticado, evidências, snapshots, logs e locks são ignorados no Git. O sistema interrompe diante de captcha, bloqueio, logout ou autenticação adicional.
