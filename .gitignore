# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node / monorepo
node_modules/
dist/
build/
.turbo/
*.tsbuildinfo

# Next.js (apps/web)
.next/
out/
next-env.d.ts

# Segredos locais. `.env` alimenta a API; `.env.tools` guarda as credenciais de
# migração e refresh, que NÃO vão para o processo que atende a internet.
# Só o exemplo (sem credencial) é versionado.
.env
.env.*
!.env.example

# Prisma
packages/database/prisma/generated/

# Logs
*.log
npm-debug.log*

# Temporários
_extracted/
*.tmp
